# Dev Workflow Project Bridge

This project is configured for the `dev-workflow` Atlas mode.

Local project instructions remain authoritative. If this file is used as a starter bridge, replace or extend it with project-specific constraints, verification commands, and ownership rules as the project develops.

The active Atlas configuration lives in `atlas.yaml`. The selected mode provides the reusable workflow assets, but it does not override human decisions, local architecture, or project-specific `AGENTS.md` rules.
